# Update 1.4.7 — 기기 간 음악 동기화 및 Apple VLC 재생

## 상태 및 기준

- 문서 개설: 2026-09-08. 버전 마감: 2026-09-10. **1.4.7 구현 완료 / 자동검사·패키징 및 서버 반영 결과는 아래 기록 / 기기 간 동기화 실기기 수용과 기존 영상·PiP 잔여 항목은 미확인**.
- [1.4.6 최종 마감](update_1.4.6.md)은 유지하고 미확인 항목을 이월한다. 사용자 요청으로 웹·Android·iOS·macOS 표시 버전을 **1.4.7**로 맞췄다. Android versionCode15, Apple build2. 최초 마감 웹 캐시는 `muzio-shell-v1.4.7-r1`, 현재 공용 웹 후속 UI는 `muzio-shell-v1.4.7-r4`.
- 자동검사·패키징·설치·실제 사용자 동작 확인을 구분한다. 기존 iPad 설치/실행과 Android 에뮬레이터 통과는 아래 실기기 수용을 대신하지 않는다.
- 확인 결과에 기종/OS, 앱 버전·산출물 해시, 사용한 파일/네트워크·초기/재진입 조건, 기대/실제 동작을 남긴다. 실패하면 해당 경로만 수정·재검하고 영향을 받은 산출물만 갱신한다.

## 현재 완료 범위

| 항목 | 구현·자동검사 | 설치·실제 동작 확인 |
| --- | --- | --- |
| Apple 영상 엔진 | 웹 컨트롤 유지, Mac VLCKit 3.7.3 / iOS 4.0.0-a24. 양쪽 최종 컴파일 통과 | Mac 일반 실행 앱 갱신 후 영상/오디오 진행 확인. iPad 수정본 설치 후 사용자 “나옴” 확인 |
| iPad 재생 시작 수정 | 검증된 캐시 URL의 IPv4 연결, 초기 paused 준비 후 ready/play 전달. 실제 VLC 시뮬레이터의 저장 위치 복원·재생·탐색·재개·reload 검사 통과 | 기본 영상·소리 확인. 장시간·서로 다른 영상 ABA·PiP는 미확인 |
| iPad 자동 PiP | VLC drawable 연결 및 명령 어댑터 검사 완료. Mac은 사용자 요청으로 제외 | 홈 → 자동 PiP → 정지/재개 → 복귀는 미확인 |
| 큐 셔플 버튼 | 화살표/X 왼쪽 글래스 원형, 활성 로즈·비활성 흰색. 공용 웹 검사 및 빌드 통과 | 웹·Apple은 서버 UI r23 사용. Android r23 번들 APK 설치 완료(9/9), 실제 터치 확인 대기 |
| Android 알림 큐 | Android 12 이하 직접 Activity 연결, 요청을 표시 acknowledgment까지 프로세스 내 보존. Kotlin 컴파일·관련 회귀 2개 통과 | 새 APK 설치·실행 완료(9/9). Android 13+ 배경에서 즉시 열기는 미확인 |

아래 체크리스트는 남은 실기기 확인을 관리한다. 과거1.4.6 검증 앱의 설치와 이번1.4.7 산출물은 구분하며, 이번 변경은 아직 커밋·푸시하지 않았다. 세부 명령·실패·최종 결과는 [Apple 검증 기록](ios_app/VALIDATION.md), [Android 검증 기록](android_app/VALIDATION.md)에 둔다.

## 기기 간 좋아요·플레이리스트 동기화

- 같은 서버에 연결한 웹·Android·iPhone/iPad·Mac에서 좋아요, 플레이리스트 생성/이름/삭제, 곡 추가/제거 및 순서를 공유한다. 서버 공용 데이터이며 계정별 분리는 없다. 재생 큐·셔플·화면 필터 등 기기별 조작 상태는 이 기능의 동기화 대상이 아니다. 재생 이력은 기존 동기화를 유지한다.
- A/B가 같은 곡을 좋아요하거나 같은 목록에 추가·제거하면 원하는 상태를 한 번만 적용한다. 두 번째 요청으로 다시 토글하지 않는다. 같은 이름의 목록을 동시에 생성하면 하나의 서버 ID로 합친다.
- 각 항목의 마지막 수정 revision을 비교한다. 서로 반대되는 오래된 요청은 거절하고 서버의 현재 상태를 유지한다. 한 기기가 연속으로 좋아요→해제를 누른 경우에는 자신의 앞선 요청 완료 뒤 후속 요청을 적용한다.
- 기존 기기별 데이터는 최초 연결 때 합친다. 어느 기기에 없다는 이유로 서버 항목을 삭제하지 않으며, 서버에 남은 삭제 기록은 이전 데이터 이관보다 우선한다. 오프라인 변경은 브라우저/앱 웹 저장소의 outbox에 보존해 재연결·재실행 때 전송한다. `local:` 기기 전용 식별자는 기기에 남기고, 공통 콘텐츠 키를 공유한다.
- Android는 새 APK의 공용 웹 번들, Apple은 서버 공용 UI를 사용한다. 알림/잠금화면에서 발생한 미전송 좋아요는 기존 네이티브 명령으로 먼저 읽고 서버 동기화를 진행한다. 새 네이티브 브리지 계약은 추가하지 않았다.
- 변경 후 약200ms에 전송하고, 화면이 보일 때15초 간격과 앱 복귀/온라인 복귀 시 확인한다. 상대 기기 반영은 통상 다음 확인 시점이다. OS가 앱을 중지한 동안 계속 실행되는 백그라운드 동기화는 아니다.
- API: `GET /api/music-sync`의 revision ETag가 같으면304·본문0바이트. `POST`는 토글 대신 명시적 값과 기준 revision을 최대128개씩 전송한다. 실제 변경이 있을 때만 config 옆 `music-sync.v1.json`을 임시 파일→fsync→rename으로 저장한다. 원본 음악·영상 및 인덱스 데이터는 전송하지 않는다.
- 격리된 실제 Go 서버 + 두 TypeScript 클라이언트로 중복 좋아요/목록 생성/곡 제거/목록 삭제, 순서, 오프라인 재실행 및 오래된 요청 거절을 확인했다. 합성 좋아요1,024개를8배치로 추가한 로컬 검사에서 저장 파일41,199바이트, 전체 요청·저장58ms였다. 이는 Mac 로컬 합성 측정이며 외부 모바일 네트워크 지연을 뜻하지 않는다.
- [ ] 서로 다른 실제 기기의 웹/Android/Apple에서 같은 서버에 연결해 중복 좋아요·목록 편집, 앱 복귀·오프라인 재연결, 네이티브 알림 좋아요를 확인한다.

## 1.4.7 최종 검사·배포·설치 — 2026-09-10

| 구분 | 결과 |
| --- | --- |
| 웹 자동검사 | 전체69파일704테스트 통과. 최초 기본 병렬 실행에서 기존 대량 로컬 목록 테스트 시간 초과 및 Vidstack 비동기 예외가 발생했다. 해당2파일11테스트를 단일 worker로 재검하고 전체도 단일 worker로 통과했다. 제품 코드·기존 assertion은 바꾸지 않았다. |
| 서버 자동검사 | `go test ./...`, `go vet ./...`, 음악 동기화 저장소·HTTP 서버 `go test -race` 통과. 디스크 실패 시 원자성, 동시 중복 요청, 영속 삭제 기록, HTTP304·입력·Origin 검사 포함. |
| 실제 API 연동 | 개인 데이터와 분리한 Go 서버 + 두 TypeScript 클라이언트의 생성/중복/삭제/오프라인/순서/304 통합 검사 통과. |
| Android | TypeScript/Vite 번들 빌드 + Gradle `:app:testDebugUnitTest :app:assembleDebug` 통과.8스위트27테스트, APK 내1.4.7-r1 확인. SM-S936N에 `adb install -r` 성공 및 MainActivity 실행·설치 버전1.4.7/vc15 확인. |
| Apple | iPhone/iPad 공용 arm64 Release archive 컴파일, 개발 서명 IPA export·서명 검증 통과. Mac arm64 최적화 컴파일·ad-hoc 서명/검증·ZIP 생성 통과. 기존 Swift5 코드의 디렉터리 열거에 Swift6 동시성 경고가 남아 있으며 컴파일 오류는 없다. |
| 운영 서버 | `scripts/deploy_local.sh`로 웹·Go 바이너리 교체 후 실제 PM2 `server-control--muzio` 재시작. 운영 HTTPS `/healthz`200, `/sw.js`1.4.7-r1, `/api/music-sync`200 및 조건부304·본문0바이트 확인. |
| 버전·변경 검사 | `VMA_VERSION=1.4.7 bash scripts/verify_version.sh`, `git diff --check` 통과. |

산출물은 `dist/releases/1.4.7-20260910/`의 Android APK, Mac ZIP, iOS archive와 `iOS/Muzio.ipa`에 모았다. SHA-256·크기·서명 범위는 같은 폴더의 `manifest.json`에 기록했다. Mac 일반 실행 경로 `dist/apple/Muzio.app`도1.4.7/build2로 갱신했다. 이번 Mac 패키지는 arm64이며 기존1.4.6 universal 산출물과 구분한다. iOS는 기존 개발 프로필을 사용했으며 만료는2026-09-14 21:00:14 KST이다. 스토어 배포·공증은 하지 않았다.

iPad Pro11(3세대)에 데이터 유지 설치와 설치 버전1.4.7/build2 조회를 완료했다. 실행 요청은 **기기 잠금 상태**로 거절되어 실제 실행·동기화 확인은 미완료다. Android는 서버 설정 화면을 확인했고, 비어 있던 입력란에 운영 서버 주소를 넣었으나 기기에서 해당 Tailscale 호스트 DNS 해석이 실패했다. **설치·Activity 실행 성공을 서버 연결·동기화 수용으로 표시하지 않는다.** 네트워크가 복구된 Android와 잠금 해제한 iPad에서 위 동기화 실기기 체크를 이어간다. 사용자 곡·좋아요·플레이리스트를 검증용으로 변경하지 않았다.

## 설치 및 UI 전달

- [x] Android 16 / SM-S936N에 최신 큐 수정과 공용 UI r23 APK를 데이터 유지 설치·실행했다(2026-09-09). 표시 버전 1.4.6/vc14, 기존 r20 마감 APK와 구분한다. 알림 큐 실제 동작 검증은 남아 있다.
- [x] iPad에 VLC4 재생 시작 수정본을 데이터 유지 설치했다(2026-09-08). 사용자가 영상·소리 출력을 확인했다. 표시 버전은 1.4.6인 개발 검증본이다.
- [ ] iPhone에도 공용 Apple 경로를 설치·확인한다. iPad 통과만으로 iPhone 수용을 완료 처리하지 않는다.
- [x] Mac의 일반 실행 경로 `dist/apple/Muzio.app`를 최종 VLC 검증본으로 갱신하고 실제 영상 진행을 확인했다(2026-09-08). ad-hoc 서명 검증 완료. 새 릴리스 ZIP·공증·App Store 배포는 아니다.

## 영상 재생 및 캐시

- [x] iPad 수정본의 기본 영상·소리 출력을 사용자 피드백으로 확인했다. 아래 장시간/조작 조합 검증과 구분한다.
- [ ] iPad/iPhone에서 A 단독 연속 재생 중 영상 끊김·무음·무한 대기가 없는지 확인. 일시정지/재생, 양방향 탐색, 영상 교체를 조합한다.
- [ ] Apple·Android 앱과 같은 기기의 웹에서 A→B→A를 비교한다. 처음 캐시 생성과 재진입, 큰 front-moov 파일과 작은 영상, 이어보기 위치를 구분해 지연을 기록한다. 앞부분 캐시는 전체 영상/임의 탐색 구간의 오프라인 캐시가 아니다.
- [ ] 웹·앱에서 영상 탭이 즉시 재생/일시정지로 동작하고 짧은 글래스 피드백만 나타나는지, 기본 중앙 UI가 겹치지 않는지 확인한다.
- [ ] iPad/iPhone에서 전체화면 진입/종료 후 검은 화면·레이아웃 손상·재생 소실이 없는지 확인한다.
- [ ] Android 폰에서 세로 잠금 상태의 가로 전체화면, 상태/탐색 바와 여백 제거, 버튼/Back 종료 후 원래 방향 복구, 반복 진입, PiP 복귀를 실제 영상으로 확인한다.
- [ ] Android 홈 자동 PiP에서 재생 중 일시정지 아이콘, 정지/재개·앱 복귀 상태 동기화를 확인한다. iPad/iPhone의 VLC 자동 PiP도 홈 전환·정지/재개·앱 복귀를 확인한다. Mac PiP는 범위에서 제외한다.

## 음악·로컬 폴더·시스템 제어

- [ ] Android AAC를 포함한 로컬/네트워크 곡에서 총 길이·진행률·탐색이 미니바/플레이어/알림에 일치하는지 확인한다.
- [ ] Android·Apple에서1,000곡 이상 폴더를 연결해 목록 선표시, 후속 메타데이터/썸네일 갱신, 백그라운드 후 재개, 폴더 권한 복원, 네트워크 곡과 혼합 표시를 확인한다. 기존 폴더를 무조건 삭제·재등록하지 않는다.
- [ ] Apple 백그라운드 음악·인터럽트/헤드폰 제거·잠금화면 앨범아트/좋아요·진행률/이력 복구를 확인한다. OS가 표시하는 시스템 버튼과 앱 구현 누락을 구분한다.
- [ ] Android 알림의 큐/이전/재생/다음/좋아요, 슬립타이머 및 재생 상태 동기화를 확인한다.

## 공용 웹·앱 UI

- [ ] 실제 터치에서 첫 long press부터 선택색이 즉시 표시되고, 마지막 선택 행 아래 선택 개수·+/×가 보이는지 확인한다. 전체 선택 해제/×로 종료한다.
- [ ] + → 기존 플레이리스트 저장 시 선택곡 모두 추가·중복 제외·선택 모드 종료. 빈 목록은 안내만 나오고 헤더 +로 직접 생성해야 하며, 생성 저장·취소·빈 이름 차단을 확인한다.
- [ ] 용량/위치 long press에는 브라우저 텍스트 선택이 뜨지 않으며 제목/가수는 선택·복사 가능함을 확인한다.
- [ ] 큐 셔플 버튼의 위치·색상, 현재 곡 유지, 해제 시 순서 복원을 실제 터치로 확인한다.
- [ ] 검색·Artist 태그·필터·정렬이 Music/Video/Image별로 앱/웹 재진입 뒤 유지되는지 확인한다. 기기 간 동기화와는 구분한다.

## 범위 제외 및 별도 결정 사항

- Apple custom AVPlayer 자동 PiP는1.4.6에서 사용자 요청으로 철회했다. 원인은 최종 특정되지 않았으며 OS상 구현 불가로 분류하지 않는다. 이후 새 사용자 요청으로 VLC 기반 iPad 자동 PiP를 별도 구현했다. AVPlayer 철회와 VLC 구현/미검증 상태를 혼동하지 않는다.
- Apple 서버 불가 상태의 cold-launch 공용 UI는 미구현이다. OS 제한으로 표시하지 않으며 이번 실기기 검증을 마쳤다는 이유로 자동 완료 처리하지 않는다. 오프라인 UI 구현, 위젯, 공증/스토어 배포는 별도 범위 결정이 필요하다.
- 아직 실행하지 않은 위 체크 항목은 모두 미완료로 유지한다.


## 개발·검증 이력

아래 내용은 각 작업 당시의 결과다. 당시의 실패·미설치·대기 표기는 상단 현재 상태를 덮어쓰지 않는다.

### Mac 실앱 검증 — 2026-09-08 초기 HTML 기준

- 1.4.7 후속 검증으로1.4.6 최종 산출물(`7bf27c0` 소스, 서버 UI r20)을 테스트했다. 이번 요청은 동작 확인이며 제품 버전/네이티브 코드는 변경하지 않았다. 새1.4.7 바이너리를 만들었다고 표시하지 않는다.
- 실제 Xcode 프로젝트의 iOS/macOS PBXSourcesBuildPhase를 비교해 **같은 Swift12개**가 포함됨을 확인했다. UI도 같은 서버 React 코드다. 별도 Apple 패치 복사 누락을 찾는 전체 중복 감사는 하지 않았다. 플랫폼 분기는 UIKit/AppKit 컨테이너, iOS 오디오 세션, Mac security-scoped bookmark 등이다.
- Mac에서 확인: 실행/저장 서버 연결·health, 네트워크 음악 재생/정지/재개 및 총 길이, 숨김 상태 음악 진행과1분 타이머 만료 정지, 로컬1001개 목록/메타데이터/커버 갱신, 추가90초 음원의 재생·드래그 탐색, 앱 재시작 후 폴더 접근/재생, 검색/정렬/오프라인·Artist 필터 유지, 플레이리스트 명시적 생성/중복 제외, 이미지 보기/닫기.
- **실패:** HTTPS 서버에 연결한 Mac 앱에서 두 원본 MP4가 각각47초 이상 Buffering에 머물고 미니바0:00/0:00·탐색 불가. 같은 시작 시각에 WebKit `CachedResourceLoader::requestResource: Not allowed to request resource`가 기록됐다. 영상 요청은 공용 videoIndexSource가 HTTP127.0.0.1 프록시로 변환하며 Mac 인덱스 캐시에 데이터가 생성되지 않았다. HTTPS 문서→HTTP loopback 요청 허용 경로가 유력 조사 대상이나, 로그만으로 구체적인 WebKit 정책 원인을 확정하지 않는다.
- 이 실패로 Mac 영상의 연속 오디오/프레임·탐색·전체화면·PiP·A→B→A 성능 판정은 차단됐다. 이미지/음악 통과를 영상 통과로 대신하지 않는다. 기존 단위 검사/컴파일은 실제 WKWebView 리소스 로딩 수용을 보장하지 않았다.
- 상세 절차·근거·한계: [Apple VALIDATION](ios_app/VALIDATION.md). 테스트용 폴더 연결과 플레이리스트는 확인 후 정리했다. 사용자 원본 파일은 변경/삭제하지 않았다. README의 예전 native 영상/PiP·Mac arm64 전용 설명을 현재 상태와 일치시켰다.

### 당시 후속 조사 항목 — VLC 전환 전

- HTTPS WKWebView의 loopback 차단과 같은 origin 원본 재생을 대조했다. localhost 변경 뒤에도 큰 영상이 멈춰 사용자 승인으로 VLC 경로로 전환했다. 아래 이력에 근거를 기록한다.
- 해당 HTML 경로의 근본 WebKit 원인이 최종 확정됐다고 표시하지 않는다. 현재 VLC 수용 체크리스트와 별도로 보존하며 이 조사를 다시 필수 작업으로 반복하지 않는다.
- Mac 실제 스피커 출력·미디어 키/인터럽트·다중선택 터치 등 남은 항목은 상단 체크리스트와 Apple 검증 기록을 따른다.

### Mac 실행 수정 및 Edge 대조

- Apple 영상 프록시 공개 주소를 `localhost`로 변경했다. IPv4 loopback 바인딩과 토큰 검증은 유지하며 이전 호스트의 숫자 주소도 웹에서 허용한다. Mac 실제 실행에서 캐시 생성과206 cache-hit를 확인했다.
- Apple 앱 시작 시 HTML 진입 문서를 서버에서 다시 받아 최신 웹 UI를 읽도록 했다. 서버의 새 JS 해시와 달리 예전 HTML/JS를 유지하던 HTTP 캐시 문제를 실제 로딩 해시로 확인했다. 공용 Swift 변경이므로 iPhone/iPad/Mac에 함께 적용된다.
- 웹 자산 r21. 아직1.4.7 개발 중이며 설치본 표시 버전은1.4.6을 유지한다.
- **미해결:** Mac Safari18.3.1 기반 WKWebView에서 큰 A/B MP4의 재생 시계가 멈춘다. 기본 HTML/WKWebView + 원본 HTTPS에서도 재현됐고 작은30초 영상은 진행됐다. Edge의 실제 웹 UI에서는 A 중간 구간5:57:50→5:59:02 진행을 확인했다. UI 공유와 브라우저 내부 재생 엔진 공유는 다르다.
- WebKit의 이후 AVAsset 바이트 전달 수정은 원인 후보이며 확정 아님. 당시 최신 WebKit 대조 또는 호스트 변경을 검토했으며, 이후 사용자 승인으로 아래 VLC 연결을 진행했다. OS 변경·Chromium 호스트 전환은 아직 수행하지 않았다. 상세 검사와 실패 범위는 [Apple VALIDATION](ios_app/VALIDATION.md)에 기록했다.
- 최종 검증: 웹 관련37개 및 웹 빌드 통과 결과 재사용, 프록시 통합 검사·Mac 컴파일·iOS Simulator 컴파일 모두 exit0. 진단용 코드는 제거했다. 큰 영상 실사용 검증이 해결되지 않았으므로1.4.7을 완료/배포 처리하지 않는다.


### VLC 공용 Apple 영상 엔진 연결

- 사용자 승인(오픈소스 엔진 시험 후 연결)에 따라 공식 VLCKit/MobileVLCKit 3.7.3을 도입했다. Mac·iPhone·iPad는 같은 `VLCVideoPlayer.swift`를 사용한다. 플랫폼별 XCFramework 링크/포함과 체크섬 검증 준비 스크립트, 배포 시 포함되는 라이선스 문서를 추가했다.
- 영상 픽셀만 네이티브로 그리고 재생·탐색·설정·테마 등 UI는 기존 웹 코드를 유지한다. Mac 창 전체화면과 웹 전체화면 상태를 연결했다. Android·일반 웹은 HTML 재생 경로를 유지한다.
- `start-paused`로 메타데이터를 준비해 재생 명령 전에 소리가 나지 않게 했다. 길이를 알기 전에 준비 완료를 보내 LIVE/0:00으로 처리되던 초기 연결 문제를 수정했다.
- VLC는 일시정지 중 탐색한 프레임을 그려도 시계 값이 이전 위치에 남을 수 있어, 새 프레임 도착으로 탐색 완료를 확인하고 정지 위치를 표시하도록 했다. 다시 재생하면 실제 시계 진행과 합쳐진다. 오래된 generation 명령과 WebKit 프로세스 종료도 처리한다.
- 실제 첫 선택에서 같은 영상의 pending load가 중복되면 Vidstack가 두 번째 `source-change`를 생략했다. `currentSrc`도 없는 이 경로에서 준비 신호가 거부되던 문제를 재현했다. 동일한 전체 URL·mediaId의 아직 준비 중인 관측 상태만 이어받고, 다른 영상·변경된 시작 위치·오류 후 재시도에는 적용하지 않는 회귀 검사를 추가했다.
- 원본 HTTPS A→B→A 엔진 검사에서 각45초간 영상 프레임·오디오 출력 버퍼·시계 진행과 일시정지/재개·앞뒤 탐색을 확인했다. Mac 앱에서도 기존 UI 위에 영상 합성, 클릭 정지/재개, 전체화면과 복귀를 확인했다. 최종 UI 재검 결과는 [Apple VALIDATION](ios_app/VALIDATION.md)에 기록한다.
- 기존 앞부분 인덱스 캐시는 유지한다. 전체 파일/임의 탐색 구간의 오프라인 캐시는 아니며, VLC 시간 버퍼 범위는 제공하지 않아 임의로 채우지 않는다. 웹 자산은 r22.
- 최초 VLC 연결 시 PiP는 미구현이었다. 이후 iPad 자동 PiP 요청에 따른 구현은 아래 후속 항목으로 구분한다.
- **당시 남은 검증(이후 기본 iPad 재생 확인):** iPhone/iPad 실제 VLC 재생, 장시간·인터럽트·헤드폰/스피커 출력, 전체화면/탐색/재진입 확인. iOS 컴파일과 Mac 성공으로 대체하지 않는다. 이번 작업은 임시 검증 빌드이며 새 배포 패키지·설치·버전 마감은 수행하지 않았다.

### iPad VLC 자동 PiP 후속 작업

- 사용자 요청으로 iPad/iOS에만 자동 PiP를 연결하고 Mac은 제외했다. iOS 의존성을 공식 VLCKit 4.0.0-a24로 고정하고 Mac 3.7.3과 경로·링크를 분리했다. 정식 출시 전 의존성이므로 기존 Mac 재생 검증을 iPad 통과로 대체하지 않는다.
- 공개 PiP drawable 프로토콜을 통해 기존 영상 픽셀을 시스템 PiP에 연결한다. 앱 안의 웹 디자인·버튼은 유지한다. 라이브러리의 자동 inline 시작을 사용하고 앱이 배경으로 내려갈 때 drawable과 재생 세션을 유지한다.
- PiP 재생/일시정지/탐색 명령은 웹과 같은 상태를 갱신하며 이전 영상의 늦은 콜백을 차단한다. 복귀 시 영상 화면을 복원하되 정지한 영상을 강제로 재생하지 않는다.
- 공개 라이브러리 API는 controller 준비와 실제 PiP 시작/종료만 전달하므로 `isPictureInPicturePossible`·실패 원인·복귀 delegate를 제공하는 것처럼 표시하지 않는다.
- 컴파일·회귀 검사·설치·실기기 자동 전환 결과는 [Apple VALIDATION](ios_app/VALIDATION.md)에 각각 기록한다. 1.4.7 배포 마감과 실기기 수용은 별도다.

### 알림바 Queue 연결 및 큐 셔플 버튼

- 공용 Queue 헤더의 뒤로가기/닫기 왼쪽에 같은 글래스 원형 셔플 버튼을 추가했다. 활성 아이콘은 로즈색, 비활성은 흰색이다. 기존 셔플 상태/설정 저장을 사용하며 현재 곡을 유지하고 해제하면 원래 큐 순서로 돌아간다. 웹·Android·Apple 공용 UI이며 웹 자산은 r23이다.
- Android 12 이하 알림의 Queue 액션은 서비스를 거쳐 Activity를 여는 대신 Activity PendingIntent를 직접 연결했다. 나머지 미디어 버튼과 축약 알림 슬롯은 유지했다.
- Android Queue 요청은 실행 시도 전에 프로세스 내 대기 상태에 저장한다. 기존 `shell.notificationIntent` 응답 계약을 유지하고 웹에서 큐를 표시한 후 acknowledgment를 보내면 지운다. 활성 앱에는 기존 resume 이벤트로 전달하고, 배경 실행이 차단되어도 앱 복귀 시 다시 전달한다. 프로세스 종료를 넘는 영구 저장은 아니다.
- **남은 한계:** Android 13+는 [SystemUI 미디어 액션](https://source.android.com/docs/core/display/media-control?hl=en)을 통해 세션 커스텀 명령을 실행한다. 이번 보완만으로 배경에서 앱을 즉시 여는 문제가 해결됐다고 판정하지 않는다. 기기 미연결로 실제 알림 탭·잠금 화면·복귀 검증은 남아 있다.
- 웹 검증: QueueDrawer 7개, App 알림 큐 연결 1개 통과. TypeScript/Vite 빌드 exit 0(기존 큰 청크 경고). SW 캐시명만 r23으로 갱신한 뒤 검증 빌드의 dist에도 복사했다.
- APK 패키징·설치는 수행하지 않았다. Android 네이티브 수정은 다음 설치본에 포함해야 한다. Apple 전용 네이티브 수정은 없다.
- Android 검증: `android_app`에서 `:app:compileDebugKotlin -x :app:buildSharedWeb` 통과 후, 요청 보존/복귀 연결을 추가하여 `./gradlew :app:testDebugUnitTest --tests com.twkim.videiomusic.playback.PendingNotificationQueueRequestTest -x :app:buildSharedWeb` 실행. 영향 Kotlin 컴파일 및 회귀 2개 통과(exit 0). 최종 로그 `/tmp/muzio-queue-android-handshake-check.log`. 웹 자산 빌드는 앞선 통과 결과를 사용했고 APK는 만들지 않았다.

### Apple 실제 영상 시작 실패 후속 수정

- 평소 실행하던 Mac 앱이 VLC 적용 전1.4.6 바이너리였음을 확인해 VLC 검증본으로 교체했다. 원래 앱은 별도 백업으로 보존했다. 실제 A 영상의 지속 프레임/오디오 진행을 확인했다.
- iPad VLC의 내부 캐시 주소 연결 거부를 확인해, 검증된 프록시 URL만 IPv4 리스너 주소로 전달한다. 토큰·포트·서버 제한은 유지한다.
- iOS VLC4에서 길이 정보만 보고 너무 일찍 준비 완료를 전달하던 초기 상태 경합을 수정했다. 초기 일시정지 준비 이후 재생하므로 저장 위치 복원 후 첫 재생이 멈추던 실제 VLC 시뮬레이터 회귀가 통과했다.
- 기존 가짜 PiP 컨트롤러 검사와 별도로 실제 UIKit 창/영상/Range 서버 재생 검사를 추가했다. 의존성의 누락된 dSYM 경로 선언도 수정했다. 상세 실패/수정/재검·설치/실기기 상태는 [Apple VALIDATION](ios_app/VALIDATION.md)에 구분 기록한다.1.4.7 마감/커밋/푸시는 하지 않았다.
- 최종 iOS/Mac 컴파일 통과, 수정본 iPad 설치 및 Mac 일반 실행 경로 갱신 완료. Mac 실제 영상/오디오 진행을 확인했고 iPad에서도 사용자가 영상과 소리가 나온다고 확인했다. 실제 iPad A→B→A·장시간·자동 PiP 검증은 아직 별도 미완료다.

### Android 수정본 설치 — 2026-09-09

- 사용자 설치 요청으로 `:app:assembleDebug` 실행, 공용 웹 자산을 포함해 exit0. APK 진입 HTML의 참조 자산 존재 및 r23 포함을 확인했다. 로그 `/tmp/muzio-android-install-20260909-build.log`.
- Android 16 / SM-S936N에 `adb install -r` 성공, 데이터 유지. `am start -W`는 Status ok / COLD, 설치 갱신 시각 21:07:15. 표시 버전 1.4.6, versionCode14 유지.
- APK: `android_app/app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `4ae18b155ae07d589f7d18255b3643472faf1df4d97748ec6b8c703e89fb155a`.
- 큐 헤더 셔플·Android 알림 큐 연결 보완 포함. 설치/실행 성공을 알림 탭·재생·PiP 실기기 동작 통과로 대신하지 않는다. 정식1.4.7 마감은 아니다.

### Android 알림 순서·큐 전환 확인 — 2026-09-09

- 사용자 요청: 큐/이전/재생/다음/하트 순서, 가능하면 큐 탭으로 알림창을 닫고 큐 표시. 불가능하면 현재 동작 유지.
- SM-S936N / Android16 실제 SystemUI에서 다음 트랙 액션이 없는 재생 상태는 `Like / 이전 / 일시중지 / Queue`로 표시됐다. 코드의 Android12 이하 정렬 및 최신 Media3 보조 슬롯 선호만으로 현재 삼성 표시 순서가 고정되지는 않았다.
- 표준 이전/다음 슬롯 예약 extras를 추가해 컴파일·설치 후 같은 기기에서 재검했으나 순서는 그대로였다. 효과 없는 extras 변경은 제거했다. 표준 재생/탐색 계약을 바꾸는 커스텀 버튼 대체나 새 접근성 권한 도입은 하지 않았다.
- 일반 앱에서 알림창 강제 닫기는 Android12+ 제한이 있다. 사용자 요청에 따라 큐 요청 보존/복귀 전달 동작을 유지하며, 자동 알림창 닫기를 구현 완료로 표시하지 않는다. [Android 정책](https://developer.android.com/about/versions/12/behavior-changes-all#close-system-dialogs), [미디어 버튼 정책](https://developer.android.com/media/implement/surfaces/mobile).
- 실기기 AX 근거 `/tmp/muzio-notification-layout.xml`, `/tmp/muzio-notification-final.xml`; 미디어 세션 `/tmp/muzio-notification-order-session.txt`. 시험 빌드 `/tmp/muzio-notification-slots-build.log`, 원복 빌드 `/tmp/muzio-notification-slots-restore.log`.

## 후속 UI 수정: 선택곡을 큐에만 추가 — 2026-09-10

- Music 목록을 마우스/터치로 길게 눌러 선택하면 기존 플레이리스트 추가와 선택 해제 사이에 **큐 아이콘 버튼**이 나타난다. 버튼에는 글자를 표시하지 않고 접근성 이름과 마우스 툴팁을 제공한다.
- 선택한 곡들을 화면 순서대로 현재 큐 끝에 추가하고 선택을 해제한다. 필터로 가려진 선택곡도 누락하지 않고 뒤에 추가한다. 플레이리스트 저장 창을 열거나 저장 데이터를 바꾸지 않는다.
- 현재 곡·재생 위치를 유지하고 자동 재생하지 않는다. 빈 큐도 지원하며, 셔플 해제 후에도 추가곡이 남는다. 기존 큐와 같은 곡을 추가할 때는 별도 큐 항목 ID를 부여해 선택·삭제가 다른 항목에 영향을 주지 않는다.
- 공용 store와 기존 `playback.queue` 명령을 사용한다. Android/Apple의 현재 항목 유지 경로 및 빈 큐의 유효 index를 확인했으며 네이티브 명령 계약은 변경하지 않았다.
- 검사: playerStore70, LibraryScreen40, nativeAudio17 = 관련127테스트 통과. 마우스/터치의 아이콘 전용 버튼, 일괄 추가·선택 해제, 재생 유지·셔플·빈 큐·네이티브 전달 회귀 포함. 네이티브 테스트 초안의 명령 기대값에는 기존 상태 조회 요청이 빠져 있어 실제 변경 명령만 검사하도록 정정 후17개 통과. TypeScript/Vite 빌드, 버전 검사, diff 공백 검사 통과.
- 공용 웹1.4.7-r2를 배포했고 운영 주소의 서비스 워커 및 실제 JS 번들에서 새 버튼을 확인했다. 프론트 변경으로 서버 프로세스 재시작은 필요하지 않았다. 웹·모바일 웹·Apple 서버 UI에 전달되며, Android는 다음 APK 패키징에 공용 변경을 포함한다. 단순 UI 변경마다 패키징하지 않는 [프로젝트 정책](AGENTS.md)에 따라 이번에는 앱 재패키징·설치를 하지 않았다. 기존 r1 APK/IPA 산출물과 위 설치 기록은 그대로 보존한다. 실제 기기 터치 수용은 별도다.

### Android r2 설치 — 2026-09-10

- 사용자 설치 요청에 따라 공용 웹1.4.7-r2를 포함한 APK를 갱신했다. `:app:assembleDebug` 및 포함된 TypeScript/Vite 빌드 통과. APK 내부 서비스 워커 r2와 실제 JS의 `selection-add-to-queue` 버튼을 확인했다.
- 연결된 SM-S936N에 `adb install -r` 성공, MainActivity 실행 요청 성공, 설치 버전1.4.7/versionCode15 확인. 기존 데이터를 지우지 않았다. 큐 버튼의 실제 터치 동작은 별도 사용자 확인 대상이다.
- 산출물: `dist/releases/1.4.7-20260910/Muzio-1.4.7-r2-android.apk`. SHA-256: `a7c7473ee2a2847183a233fac1d90dcd430209ea6d80f131bf904c2b7d9f4e11`. 이전 r1 산출물은 보존했다. 앞선 APK 갱신 대기 기록은 이 설치로 해소됐다.

### 웹 상단 디자인 실험 롤백 — 2026-09-10

모바일 화면 문제로 사용자 요청에 따라 r3의 웹 전용 상단 두 섬 배치를 철회했다. 필터·검색은 원래 중앙 섬으로, 설정은 원래 위치로 복구했다. 큐 추가 아이콘과 기존 기능은 유지하며, 캐시를1.4.7-r4로 갱신해 복구 화면을 전달한다. 앱 재패키징은 하지 않았다.

### Android 런처 아이콘 내부 비율 수정 — 2026-09-10

- 사용자 첨부 기준 iPad 로고 폭은 약54%, Android는 약83%였다. Android adaptive foreground와 monochrome 벡터에 중심(256,256) 기준2/3 스케일을 적용했다. 배경·로고 경로·색상, Apple 및 웹 아이콘은 유지했다. 로컬 아이콘 생성 스크립트에도 같은 보정을 반영했다.
- `:app:assembleDebug` 및 리소스 merge/process, 공용 웹 빌드 통과. 사용자 요청의 수정 반영을 위해1.4.7/versionCode15 APK를 갱신하고 연결된 SM-S936N에 `adb install -r` 성공. 데이터 삭제 없음.
- 설치 후 실제 앱 정보 화면 스크린샷에서79px 아이콘 안의 로고43×34px(폭54.4%, 높이43.0%)를 확인했다. 첨부 iPad의54.4%/42.3%와 근접한다. 런처 폴더에서의 최종 사용자 확인과 테마 단색 렌더는 별도이며, 검증 후 홈 화면으로 복귀했다.
- 산출물 `dist/releases/1.4.7-20260910/Muzio-1.4.7-icon-android.apk`, SHA-256 `5d3350fe7e1c01a8ef9202cf4912e2c39966d1bfba3b2617caf7eb60200f1948`. 이전 패키지는 보존했고 최신 큐 버튼 및 r4 상단 롤백 UI도 포함된다.

### 앱 재실행 후 미니바 영상 이어보기 보완 — 2026-09-11

- Android에서 신고된 증상: 앱을 다시 열면 미니바에는 저장 시간이 표시되지만, 영상 화면에 들어가지 않고 재생하면 실제 영상은0초에서 시작했다. 저장 시간 전달과 실제 탐색 완료를 구분해 공용 플레이어 경로를 검사했다.
- 회귀 테스트로 초기 `can-play` 이후에도 탐색 범위가 준비되지 않아 저장 위치 요청이0초로 제한되는 상황을 재현했다. 기존 엔진은 이때의 `seeking`을 수동 탐색으로 오인해 이어보기 목표를 취소했다. 초기 내부 탐색은 목표를 유지하고, provider의 progress/time 이벤트에서 준비된 범위에 다시 적용하도록 보완했다. 재시도는250ms 간격으로 제한하며 별도 타이머를 만들지 않는다.
- 사용자가 직접0초 또는 다른 위치를 선택하면 그 요청을 우선한다. 목표 도달 후에는 재시도하지 않고, 목표 근처 키프레임에서 정상 진행하는 영상도 반복 탐색하지 않는다. 영상 화면에서 미리 준비한 재생 경로에도 같은 내부 탐색 구분을 적용했다.
- 공용 store의 저장 시간 복원→미니바 재생 및 영상 화면 사전 준비 경로를 실제 session/engine과 연결해 검사했다. iOS(iPhone/iPad 공용)·macOS의 실제 Vidstack/네이티브 provider 연결에서도 탐색 범위가 늦게 도착한 후 `video.seek(24)` 명령이 호스트로 전달되는 것을 확인했다. Android는 공용 HTML 영상 재생을 사용하며, Apple의 기존 VLC 초기 준비·탐색 계약은 유지했다. 네이티브 소스/명령 계약은 변경하지 않았다.
- 자동 검증: player/progress/core playback27개 파일379테스트 통과. TypeScript/Vite 빌드 및 `:app:assembleDebug` 성공, diff 공백 검사 통과. 테스트의 DOM 요소가 실제 문서에 붙지 않아 발생한 초기 fixture 실패는 연결 상태를 실제 앱과 맞춰 해결했으며, 제품의 분리된 요소 처리 로직은 변경하지 않았다.
- 공용 웹1.4.7-r5 배포 완료. 운영 주소의 서비스 워커 버전과 실제 player JS 청크가 빌드 결과와 일치함을 확인했다. 프론트 변경으로 서버 프로세스 재시작은 하지 않았다. 웹·모바일 웹·Apple 서버 UI에 전달된다. 이번에는 Apple 네이티브 재컴파일·재패키징을 하지 않았다.
- Android 수정 APK: `dist/releases/1.4.7-20260910/Muzio-1.4.7-resume-android.apk`, SHA-256 `4fdbef0b784a2c5b1ffc2d90f52993a4ac358f1b4b55e279e9d1c92ef829ba72`. APK 내부 r5와 수정 로직을 확인했다.1.4.7/versionCode15 유지, 이전 산출물 보존. **기기 미연결로 설치 및 실제 앱 종료→재실행→미니바 재생 확인은 미완료**이며, 자동 테스트 통과를 Android/Apple 실기기 수용으로 대신하지 않는다.

### 영상 시스템 재생 제어·가수 필터 이동 — 2026-09-11

- Android 영상은 기존 WebView 디코더를 그대로 사용하면서 Media3의 영상용 세션을 서비스에 연결했다. 제목·길이·진행 시간·재생 상태를 전달하고 재생/일시정지, 앞뒤10초 이동, 위치 탐색 명령을 현재 영상의 공용 store로 돌려보낸다. 음악 큐·좋아요용 세션과 제어 대상을 전환하며 두 번째 영상 스트림을 열지 않는다.
- Android 알림 전달은 위치가 바뀐 매초 및 상태 변화 시에만 갱신한다. 다른 영상에 대한 늦은 명령, 이전 WebView의 세션 해제는 차단한다. 웹에서 세션을 지운 결과에 Pause를 되돌려 보내 동일 영상의 재시작을 취소하지 않도록 했다. 잠금/배경 전환 시 활성 영상 및 명령 채널을 유지하고 Activity 종료 시 정리한다. PiP를 닫거나 앱을 제거하는 실제 OEM 동작은 실기기 검증 대상이다.
- iPhone/iPad·Mac의 VLC 영상은 `MPNowPlayingInfoCenter`에 영상 제목·시간·재생 속도를 전달하고 `MPRemoteCommandCenter`의 재생/일시정지·토글·앞뒤10초·위치 탐색을 처리한다. 음악과 영상의 소유권 전환 시 등록된 명령 대상을 해제하며, 영상에는 음악 큐/좋아요 명령을 노출하지 않는다. 메타데이터는 영상 제목과 시간이며 영상 썸네일의 네이티브 artwork 추가는 포함하지 않는다.
- 새 Apple 호스트는 `nativeVideoSession` capability를 노출한다. Android는 명령 지원 여부를 먼저 확인한다. 새 네이티브 세션이 있으면 웹 MediaSession이 덮어쓰지 않고, 기존 설치본은 기존 웹 경로를 유지한다. API 근거: [Android MediaSession](https://developer.android.com/reference/androidx/media3/session/MediaSession), [Apple Remote Command Center](https://developer.apple.com/documentation/mediaplayer/mpremotecommandcenter).
- 공용 음악 전체 플레이어에서 가수명을 버튼으로 연결했다. 누르면 재생 상태를 유지한 채 Music 목록으로 이동하고 해당 가수 필터를 선택하며 검색창을 열어 포커스를 준다. 기존 검색/다른 필터는 해제하고 정렬은 유지한다. 가수 정보가 없으면 기존 설명 텍스트를 표시한다. 웹·Android·Apple 공용 변경이다.
- 검증: 관련 웹119테스트 통과, TypeScript/Vite 빌드 성공. 확대 검사에서 대용량 로컬 목록 테스트의5초 시간 초과와 Vidstack의 비동기 slider 예외가 각각 발생했으나, 해당2개 파일11테스트 재검은 변경 없이 통과했다. 이를 해결하기 위해 관련 없는 제품 코드나 제한 시간을 바꾸지 않았다. 확대 검사를 최초부터 통과한 것으로 표시하지 않는다.
- Android Kotlin 컴파일 및 기존 단위 테스트29개(영상 세션 소유권2개 포함) 통과. iOS Simulator(iPhone/iPad 공용)·macOS Debug 컴파일 성공. iPad 시뮬레이터에서 실제 VLC 재생/저장 위치 탐색 회귀 및 새 Now Playing 메타데이터/명령/해제 검사가 통과했다. Mac에서도 동일 Now Playing 검사가 통과했다. OS 화면의 실제 버튼 배치·잠금/해제·Bluetooth·배경 장시간 재생은 실기기 미검증이다.
- 웹1.4.7-r6 배포 완료. 운영 서비스 워커와 실제 가수 이동/영상 제어 JS 청크가 빌드와 일치함을 확인했다. 서버 프로세스 재시작은 필요 없어 하지 않았다.
- [패키징 정책](AGENTS.md)에 따라 이번 기능 작업에서는 배포 APK/IPA/Mac ZIP을 다시 패키징·설치하지 않았다. Android 기능·가수 링크는 다음 APK에 포함해야 하며, Apple 가수 링크는 서버 UI로 전달되지만 새로운 영상 시스템 제어에는 새 네이티브 설치본이 필요하다. 앞선 r5 이어보기 APK에는 이번 기능이 없다. 버전1.4.7 유지.

### iPad 시스템 영상 제어 수정본 설치 — 2026-09-11

- 사용자 설치 요청으로 현재1.4.7/build2 iPhone/iPad 코드를 실제 기기용 Debug로 빌드·서명했다. `xcodebuild` 성공, `codesign --verify --deep --strict` 통과. 로그 `/tmp/muzio-system-ipad-build.log`.
- 연결된 iPad Pro11(3세대)에 앱 삭제 없이 `devicectl device install app` 성공. 설치 결과 `/tmp/muzio-system-ipad-install.json`, 기기 앱 목록1.4.7/build2 확인. 영상 Now Playing/remote controls 및 공용 서버 UI의 가수 필터 이동 적용 대상이다.
- 설치 후 기기가 다시 잠겨 자동 실행은 Locked/RequestDenied로 실패했다(`/tmp/muzio-system-ipad-launch.json`). 설치 성공과 실행/실기기 재생 검증은 구분한다. 화면을 끈 상태의 장시간 소리 재생은 아직 검증되지 않았으며, 이번 설치를 해당 중단 문제의 해결로 표시하지 않는다.

### Mac Dock 아이콘 크기 보정 — 2026-09-12

- 배경·로고 전체에 Mac 전용 투명 여백을 추가했다. 첫 80% 축소안은 약간 작다는 피드백에 따라, 로컬 macOS 계산기 ICNS 실측에 맞춰 128px에서는 12px 여백·104px 본체, 256px에서는 25px 여백·206px 본체로 보정했다. 이는 기본 앱과의 실측 비교이며 Apple이 일률적인 축소율을 규정했다는 의미는 아니다. 내부 비율·색상과 다른 플랫폼 아이콘은 유지했다.
- 기존 로컬 생성 스크립트는 크기별 픽셀 정렬과 작은 아이콘의 시각 보정을 적용한다. 수동 ICNS 청크 조립을 Apple `iconutil` 인코딩으로 변경하여 작은 크기의 투명도 손실을 해결하고 Retina 표현을 포함했다.
- 최종 ICNS의 16–1024px 전체 표현에서 투명도 유지 확인, 16·32·128·256px에서 알파 50% 기준 본체 경계가 계산기와 일치함을 확인했다. 128px 렌더 확인 및 macOS Debug 빌드 성공(`/tmp/muzio-mac-icon-build.log`).
- 사용 중인 `dist/apple/Muzio.app`의 아이콘 리소스만 교체하고 ad-hoc 재서명·deep strict 검증을 완료했다. 실행 중 재생에 영향을 주지 않도록 앱을 종료하지 않았으며, Dock의 새 아이콘 확인에는 앱 완전 종료 후 재실행이 필요하다. 배포 ZIP 재패키징은 하지 않았다.

### 동영상 목록 썸네일 카드 — 2026-09-12

- 동영상 목록을 16:9 썸네일과 두 줄 제목의 반응형 카드로 변경했다. 휴대폰은 한 열, 넓은 화면은 여러 열로 표시한다. 재생시간 배지(메타데이터가 있는 경우), 이어보기 진행률, 세로 더보기 아이콘을 제공하며 기존 재생·길게 누르기·선택 동작을 유지한다.
- 가상 스크롤을 열 수와 카드 높이에 맞춰 계산하고 ResizeObserver로 컨테이너 너비 변경을 반영한다. 음악·이미지는 기존 목록을 유지한다.
- LibraryScreen 42개 검사 통과: 500개 영상의 마지막 항목 도달 및 한 열로 변경 후 마지막 항목 도달 포함. 테스트 환경의 animation-frame 대기 시간 초과를 타이머 대기로 수정한 뒤 전체 파일을 다시 통과했다. TypeScript/Vite 빌드 및 로컬 배포 완료, PWA 캐시 1.4.7-r7.
- 실제 데이터로 데스크톱 및 390px 휴대폰 너비의 카드 렌더 확인. 공유 웹 UI 변경이며 네이티브 계약 변경은 없다. Android는 Gradle buildSharedWeb에서 다음 빌드에 자동 포함되며 이번 작업에서는 APK 패키징·설치를 하지 않았다. Apple 앱은 서버 UI를 새로 로드하면 반영된다.

### 더보기 제목 복사 및 선택 범위 수정 — 2026-09-12

- 음악·비디오 더보기 메뉴를 `누르면 복사됨` 항목 하나로 교체했다. 메타데이터 제목이 있으면 해당 제목을, 없으면 파일명을 복사한다. 성공·실패를 구분해 표시하며 Clipboard API를 사용할 수 없는 웹뷰에는 기존 복사 명령을 시도한다. 재생목록 추가는 길게 누르기 선택 도구에서 계속 제공한다.
- 다른 항목의 더보기를 열면 이전 메뉴를 닫아 중복 팝업을 방지한다. 종류 전환 시 목록을 재생성하고 선택을 초기화하며, 해제된 행의 길게 누르기 타이머도 취소한다. 검색어·필터가 바뀌면 기존 선택을 해제한다.
- LibraryScreen 45개 검사 통과: 음악/영상 제목 복사와 단일 메뉴, 영상→음악→이미지 선택 해제, 검색어 및 필터 적용 시 해제 포함. TypeScript/Vite 및 로컬 배포 성공, PWA 캐시 1.4.7-r8 확인. 네이티브 계약 변경 없음. Android 설치 앱은 다음 패키징에 공유 UI가 포함되며 이번에는 APK 설치를 하지 않았다. 실제 기기 클립보드 동작은 별도 확인이 필요하다.
